# Fixed-root paired KO pilot

2026-09-24. Offline counterfactual experiment, not an FT7 and not a new Native policy.

Cases fixed before outcomes: G1 F48 (delayed setup), G1 F120 (attack vs ending geometry), G1 F192 (public pending garbage). Reconstruct original verified run 35765171232/game 1 using its recorded public decisions in the existing authority arena; verify all decision snapshots plus final KO/frame/totals. Freeze both players' states at the decision boundary. F120's recorded common root Hold, if present, is applied to the environment before freezing so the two placement candidates share the same public snapshot. It is not an extra candidate advantage.

A = recorded Native top-1 at the selected snapshot. B = unchanged Legacy adapter top-1 on that same snapshot. Keep equal actions if any; no replacement after observing outcomes. Standalone Hold roots get fresh Legacy analysis before placement. Only the first action differs; all later requests on BOTH players use the unchanged snapshot-only Legacy profile (200k node cap). This measures root value conditional on that continuation policy, not optimal root value or Native strength.

Two independent synthetic scenario seeds per case (51001, 51002), each replayed in normal and mirrored seats. Each scenario/orientation runs both A and B: 8 matches/case, 24 total. Mirror both states, synthetic streams and focal identity together; mirror is a seat-parity check, NOT a new independent future sample. Require normal/mirrored gameplay outcomes to match after relabeling; mismatch is technical and blocks interpretation. No hidden replay sequence/hole RNG survives freezing. Preserve current + NEXT 5 and all public fields. Unknown tail is IID uniform robustness assumption, no bag inference; holes use newly seeded authority hole generator. Same exogenous streams for A/B, consumed according to actual spawn/Hold/garbage transactions, not forced to stay at the same draw index after branches diverge.

Real existing pending/confirmed travel and both boards are retained. Future opponent attacks are generated by its actual public-only policy. Authority state is private to runner; only detached visibleState reaches any bot. No actual replay opponent future actions are used after branching.

Fixed cadence 24 frames/placement, strict top-1, validated direct commits, no fallback. KO is the only scored outcome; maxFrames=null. Watchdog 360000 additional frames is technical, never a win. Simultaneous KO unscored: exclude that A/B pair and report it; this bounded pilot does not automatically replace samples. Any technical/certificate/parity failure stops that case. Infrastructure timeout likewise means incomplete.

Primary report: paired focal win/loss A vs B per independent seed, discordant pairs (A-only win/B-only win), plus seat parity. Attack and ending geometry cannot award wins. With only two independent futures/case, even two same-direction discordances are a follow-up signal, not statistical proof. All concordant outcomes = no KO discrimination in this sample, not equivalence of root value. No automatic tuning, extra samples, FT7 or promotion.

Execution: three case jobs, full tests before battle, <=300 minutes/case with <=270-minute battle step. Save per-match results and failure dumps; ntfy completion/failure after all cases. No continuous polling.
