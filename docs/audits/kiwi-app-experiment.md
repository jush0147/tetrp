# Last-chance generated APP experiment

User-requested final offensive attempt, 2026-09-24. This intentionally departs from sent − L − U − H. Opt-in objective `generated-app` scores expected cumulative generated attack / speculative placement count, retaining the existing −1e6 dead-state penalty. Generated includes attack consumed by cancellation; Hold itself is not a placement. L/U/H are zero; no mini penalty, TSD bonus, B2B bonus, or tie-breaking board bonus. Equal scores keep existing deterministic ordering. TT uses accumulated generated reward at the same depth. The browser default remains sent-safety.

Finite visible information and first uncertain frontier cutoff remain; this ratio is the searched prefix APP, not a proven estimate of full-game APP. Premature frontier / finite-horizon exploitation and suicidal stacking beyond the horizon are accepted risks of this deliberately narrow experiment. No invented opponent attack or hidden queue. Frontier extension is disabled and rejects this objective.

One FT7 vs unchanged Legacy, seed 2026092403, 1M geometry budget (previously tested budget), original beam32, original horizon and node cap. The higher previously tested budget avoids deliberately forcing the new objective into the usual two-ply 100k ceiling; this is not a single-variable comparison to the original 100k FT7. No simultaneous root-fair or diversity changes. Same strict placement authority, 24 frames/placement, reseed/seat swap each game, KO only, no scored frame-cap outcome, technical failure stops unscored, simultaneous KO unscored. A workflow timeout is incomplete, never a win.

Actions runs unattended; result/partial artifacts retained and ntfy topic just_a_kiwi_for_tetrp receives completion/failure and objective label. No parameter sweep or automatic retry with new weights. First inspect correctness, then report KO score; APP is diagnostic/objective only and cannot award a game. Taking some rounds is a pilot signal, not evidence of superiority or promotion.
