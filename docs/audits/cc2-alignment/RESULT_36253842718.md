# Lock timing 修正驗收

2026-09-27（台北）。[run 36253842718](https://github.com/jush0147/tetrp/actions/runs/36253842718)，Tetrp commit `0d8d3ec36b405e78a8eec92c99cf44b2ce6ab74c`。

**保留這個獨立修正。** 真正Rust baseline／lock-timing兩組皆完整執行，配對gate通過，ntfy成功。Artifact已下載到 `.cache/cc2-clock-run-36253842718/`。

- 同一65 fixtures：baseline 58一致／7差異；candidate 62一致／3差異。
- 恰好消除預先指定4例：`empty/fractional-multiplier`、`empty/activation-24-hole-0/4/9`。
- 其餘61個comparisons完整不變；兩組cases完全一致。這些條件由 `kiwi-cc2-clock-gate.js` 在Actions強制assert。
- 剩餘3例與baseline相同：`empty/charged-base3`、`empty/inactive-head`（queue scan）；`empty/partial-storage-top`（40bit截斷）。
- Rust Forecast tests：9 passed／0 failed，含2個新增test functions，涵蓋23/24/25啟用跨兩手、不可重複tank、clear blocking、inactive cancellation後不可復活pending。
- 先前public-tree allowlist補正的 `b2a08e4d2c839ed35f1d5d38161b1c0de1791529` 一般engine-tests workflow也已success。

此結果證明的是已測範圍內的lock-boundary修正，不是完整TL parity或強度提升。Production WASM仍未替換，Hold／movegen／spawn KO尚未認證。

下一步只修queue scan：沿封包順序跳過未ready封包，實際扣除被選中的封包，保留cap與cancellation順序；增加中間封包部分／完全消耗和跨兩手案例。以已通過lock-timing版本為對照，預期原65例僅剩storage差異；不混入40bit修正，不跑FT7。
