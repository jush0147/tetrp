# Queue scan 驗收

2026-09-27；[run 36254310327](https://github.com/jush0147/tetrp/actions/runs/36254310327)，commit `6161f2655e0311c9ab228c8b028dca9122f79741`。

**保留queue-scan修正。** 兩個真正Rust版本、paired gate、ntfy均success；一般engine-tests run36254310333亦success。Artifact下載於 `.cache/cc2-queue-run-36254310327/`。

- 69個相同fixtures：lock-timing對照5差異；queue-scan候選1差異，68個一致。
- 修正恰好4例：charged-base3、inactive-head、queue-partial-middle、queue-complete-middle；其餘65個comparisons完整不變（Actions gate強制assert）。
- 原有65例由3差異降至1；新增4例全部一致。
- 唯一剩餘 `empty/partial-storage-top`，僅cols不同，候選仍保留不應存在的bit40。
- Rust Forecast 12 tests passed／0 failed，包含3項queue tests：中間packet部分／完全消耗跨兩手，以及inactive隊首FIFO cancellation。Timing tests繼續通過。

不是完整TL parity或strength evidence。未驗證部分仍包括movegen/provenance、Hold、spawn/clutch KO，以及failed insertion時pending是否正確保留。Production WASM未替換。

下一步維持計畫：處理40bit storage截斷，補頂端不同分布、連續插入與failed-insertion對照。後者若確認是另一個pending交易差異，必須單獨標記，不把它偷偷混成clipping修正。先把transaction做好，再進movegen／Hold／spawn，不開FT7。
