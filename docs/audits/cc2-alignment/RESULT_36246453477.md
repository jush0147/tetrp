# 真正 Rust transition 診斷結果

2026-09-26；[run 36246453477](https://github.com/jush0147/tetrp/actions/runs/36246453477)。Tetrp `1dec90dd9ed76802267b3107ffa23b627940f5e9`；CC2 `2e243242b674d57491f99b445f75e35fc48a0e26`。

## Verdict

診斷、Rust編譯、artifact、ntfy jobs均成功。65個請求完整輸出，Rust stderr空白，沒有driver rejection；**58個被比較的transition結果一致，7個不一致。不是模型parity通過，也不是strength evidence。**

下載位置：`.cache/cc2-transition-run-36246453477/cc2-transition-36246453477-1/`。重新執行 comparator 與 artifact `comparisons.json` 完全一致；65個遠端 cases 與先前本機 cases deep-equal。檢查 instrumentation diff：只有getter、observer module與事件hooks，沒有修正 gameplay semantics。

## 已證實的三類差異

|原因|案例|Tetrp / CC2|影響|
|---|---|---|---|
|+24啟用被提前算進+23 lock|`empty/activation-24-hole-0/4/9`，3例|當手 tank 0 / 2|Rust提早入垃圾、消耗pending、抬高盤面|
|同一時間邊界，與原有active packet交互|`empty/fractional-multiplier`|tank 3 / 8|先有3行active，另外7行+24啟用；CC2錯把後者也納入當手cap8。名稱雖含multiplier，根因不是倍率|
|遇到inactive隊首便停止|`empty/inactive-head`|tank 3 / 0|Tetrp跳過inactive封包，仍接後面的active；CC2不接|
|同一隊列掃描問題，出現在中間封包|`empty/charged-base3`|tank 8 / 2|Tetrp接完2行，跳過未active的8行，再接後面6行；CC2接2行後停止。當手沒clear，沒有Surge釋放；根因不是base3|
|storage頂端沒截成40bit|`empty/partial-storage-top`|都tank1，但CC2 column0殘留`1<<40`|Tetrp丟掉舊頂格，Rust留下盤面外bit，後續height／state hash等會看見不同state|

前兩類共6例同時影響cols、garbageRows、pending、tanked；最後1例僅cols不同。Inactive-before-active為刻意構造的conditional public state，未證明它在目標arena自然出現的頻率；不以這兩例估計實際敗局貢獻。

## 這次沒有看到的差異

65例的generated、cancelled、sent、attack packet順序、raw B2B、combo、piece counter、clear/AC、elapsed都一致。Opener第14/15顆、cumulativeSent門檻、Surge/base3、AC、garbage bonus與倍率的控制案例未出現上述欄位差異。

最大lockMultiplier數值差為 `2.220446049250313e-16`，小於預先設定1e-12比較容忍，整數attack全部一致。不是對所有浮點floor臨界值的認證。

Spin/cells亦一致，但兩邊吃的是已由Tetrp證明的同一placement；**此結果不驗證CC2 movegen／spin provenance。** Hold lifecycle、empty-Hold cursor、spawn/clutch KO與DAG尚未驗證；不能把58/65當作「TL規則已對齊89%」。三個差異也不是Native 0–7的歸因，測的是現行Legacy/CC2內部模型。

## 接續的最小修正

保持原路線，先修Forecast，不動evaluator或DAG策略：

1. 第一個獨立修正：以`elapsed_before + cadence - 1`處理本次lock的activation/tank；after-state仍前進完整cadence。+24封包在新decision可active，但不能回溯上一個lock。預期只消除4個clock案例，另外3個差異仍存在，便於查修正是否越界。
2. 再獨立修正tank掃描：跳過not-ready，但按原序取消、保留cap，不誤用隊首consume去扣被跳過的封包。增加中間封包部分消耗、完全消耗、跨兩手測試。
3. 最後修40bit board/garbage mask截斷；補頂格不同分布、連續插入、fulltop拒絕且不錯扣pending的案例。

每步用實際Rust runner重跑，不用JS仿寫確認。新增cases要包含多手continuation與failed insertion；当前65例沒有完整覆蓋這兩者。修完transaction後才擴movegen、Hold、spawn；尚不接新production artifact、不開FT7。
